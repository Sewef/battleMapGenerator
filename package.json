{
  "name": "battlemap-generator",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --configLoader runner",
    "build": "tsc && vite build --configLoader runner",
    "test:generation": "vite build --configLoader runner --ssr src/generation/generation-test.ts --outDir dist/generation-test && node dist/generation-test/generation-test.js",
    "preview": "vite preview --configLoader runner"
  },
  "dependencies": {
    "@types/d3-delaunay": "^6.0.4",
    "d3-delaunay": "^6.0.4"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^7.0.0"
  }
}
